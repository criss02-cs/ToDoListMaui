<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ToDoListMaui.Views.HeaderView"
             x:Name="this">
    <StackLayout BackgroundColor="Transparent">
        <Rectangle BackgroundColor="HotPink" WidthRequest="50" HeightRequest="50" Rotation="15"/>
        <!--<VerticalStackLayout Padding="0,80,0,0" Rotation="-15">
            <Label 
                x:Name="TitleLabel"
                FontSize="50"
                TextColor="White"
                FontAttributes="Bold"/>
            <Label 
                x:Name="SubTitleLabel"
                FontSize="30"
                TextColor="White"/>
        </VerticalStackLayout>-->
    </StackLayout>
</ContentView>
